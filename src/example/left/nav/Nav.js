import React, { Component } from 'react';
import Env from '../../../app/Env';

export default class Nav extends Component {
  render() {
    return (
      <nav>
          <Env/>
      </nav>
    );
  }
};